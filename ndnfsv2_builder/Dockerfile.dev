FROM golang:1.7-alpine

RUN apk update && apk add git

RUN go get -d github.com/nexenta/nedge-docker-nfs/... && cd $GOPATH/src/github.com/nexenta/nedge-docker-nfs && git checkout v2_plugin && go get github.com/nexenta/nedge-docker-nfs/...
