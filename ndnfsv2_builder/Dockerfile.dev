FROM golang:1.7-alpine

RUN apk update && apk add git

RUN go get -d github.com/nexenta/nedge-docker-nfs/... && cd $GOPATH/src/github.com/nexenta/nedge-docker-nfs && git checkout multiple_mount && go get github.com/nexenta/nedge-docker-nfs/...
